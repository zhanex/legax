{
  "mcpServers": {
    "legax": {
      "command": "node",
      "args": [
        "./scripts/mcp-server.mjs"
      ],
      "note": "Local stdio MCP server that relays Legax messages and agent-mediated approval requests to configured transports. It uses the default Legax config path unless LEGAX_CONFIG is set by the operator."
    }
  }
}
